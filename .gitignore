# User data: real instruments and the real ledger. Never committed.
# Seed data under data_store/seed/ IS committed -- it is the demo and test
# fixture, and it is synthetic.
portfolio/data_store/user/
data_store/user/

# Spike output: raw provider responses, may contain account-shaped metadata
# and is regenerated on every run.
spike/results/

# Build artefacts. portfolio_tracker.egg-info/ was committed by accident while
# verifying the packaging fix, and its SOURCES.txt is rewritten by every
# `pip install -e`, so it shows up as a spurious diff on any machine that has
# installed the project.
*.egg-info/
build/
dist/

__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.DS_Store
