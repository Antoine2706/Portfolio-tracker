/* Transactions page — placeholder. Props: { route, snapshot }. */
import { makePlaceholder } from "/static/pages/_placeholder.js";

const TransactionsPage = makePlaceholder({ title: "Transactions", subtitle: "The ledger: buys, sells, dividends and fees, with import and export.", icon: "transactions" });
export default TransactionsPage;
