/* Performance page — placeholder. Props: { route, snapshot }. */
import { makePlaceholder } from "/static/pages/_placeholder.js";

const PerformancePage = makePlaceholder({ title: "Performance", subtitle: "Time-weighted return, drawdowns, monthly returns and contributions against the benchmark.", icon: "performance" });
export default PerformancePage;
