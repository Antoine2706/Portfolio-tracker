/* Simulator page — placeholder. Props: { route, snapshot }. */
import { makePlaceholder } from "/static/pages/_placeholder.js";

const SimulatorPage = makePlaceholder({ title: "Simulator", subtitle: "What-if: cash changes, target weights and presets, with the before/after risk diff.", icon: "simulator" });
export default SimulatorPage;
