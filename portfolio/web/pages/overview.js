/* Overview page — placeholder. Replace this file with the real page; keep the
   default export signature: OverviewPage(props) where props = { route, snapshot }. */
import { makePlaceholder } from "/static/pages/_placeholder.js";

const OverviewPage = makePlaceholder({ title: "Overview", subtitle: "Value, P&L, allocation and what needs attention today.", icon: "overview" });
export default OverviewPage;
