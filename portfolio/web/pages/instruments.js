/* Instruments page — placeholder. Props: { route, snapshot }. */
import { makePlaceholder } from "/static/pages/_placeholder.js";

const InstrumentsPage = makePlaceholder({ title: "Instruments", subtitle: "The universe: ISIN resolution, venues, symbols and notes.", icon: "instruments" });
export default InstrumentsPage;
