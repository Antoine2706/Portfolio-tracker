/* Risk page — placeholder. Props: { route, snapshot }. */
import { makePlaceholder } from "/static/pages/_placeholder.js";

const RiskPage = makePlaceholder({ title: "Risk", subtitle: "Where the risk sits versus where the capital sits: divergence, correlation, VaR and scenarios.", icon: "risk" });
export default RiskPage;
