/* Settings page — placeholder. Props: { route, snapshot }. */
import { makePlaceholder } from "/static/pages/_placeholder.js";

const SettingsPage = makePlaceholder({ title: "Settings", subtitle: "Data mode, provider, cache and the risk window.", icon: "settings" });
export default SettingsPage;
